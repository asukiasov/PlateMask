/**
 * Count accepted raw chars left of `caret` in a formatted value.
 * @param {string} value
 * @param {number} caret
 * @returns {number}
 */
export function rawIndexAt(value, caret) {
  void value, caret;
  throw new Error('rawIndexAt: not implemented (Phase 2)');
}

/**
 * Caret position in the formatted value right after raw char `k`.
 * @param {number[]} map  from FormatResult
 * @param {number} k
 * @returns {number}
 */
export function caretFromRaw(map, k) {
  void map, k;
  throw new Error('caretFromRaw: not implemented (Phase 2)');
}
