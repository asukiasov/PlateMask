import { describe, it } from 'vitest';

describe('normalize', () => {
  it.todo('uppercases');
  it.todo('strips diacritics ("é" → "E")');
  it.todo('drops symbols except separators');
  it.todo('applies transliteration map (Cyrillic "А" → Latin "A")');
});
